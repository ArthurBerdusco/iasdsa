// components/SectionHeader.tsx
// Mantido como re-export para compatibilidade com imports existentes.
// A implementação real e padronizada vive em `ui/SectionHeader.tsx`.
export { default } from "./ui/SectionHeader";
